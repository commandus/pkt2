#!/bin/sh
# sudo su - postgres
# ./mkdbschema

DBHOST=localhost
POSTGRES_VERSION=9.5
POSTGRES_SCHEMA=iridium
POSTGRES_PORT=5432
POSTGRES_USER_NAME=imz
POSTGRES_USER_PASSWORD=LC50vbHc

echo $DBHOST:$POSTGRES_PORT:*:$POSTGRES_USER_NAME:$POSTGRES_USER_PASSWORD > ~/.pgpass && chmod 0600 ~/.pgpass

createuser $POSTGRES_USER_NAME -W

# ALTER USER imz WITH PASSWORD 'LC50vbHc';

# DROP ROLE $POSTGRES_USER_NAME;
# CREATE ROLE $POSTGRES_USER_NAME PASSWORD $POSTGRES_USER_PASSWORD CREATEDB INHERIT LOGIN;
# DROP DATABASE $POSTGRES_SCHEMA
# psql -h localhost $POSTGRES_USER_NAME iridium -w -c "CREATE DATABASE onewayticket WITH OWNER = onewayticket ENCODING = 'UTF8';"

exit 0